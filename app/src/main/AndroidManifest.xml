<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mobileapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".AddStudents"
            android:exported="true" />
        <activity android:name=".DisplayStudents" />
        <activity android:name=".DisplayCourse" />
        <activity android:name=".StudentDash" />
        <activity android:name=".AddCourse"
            android:exported="true"/>
        <activity android:name=".StaffDash" />
        <activity
            android:name=".AddStaff"
            android:exported="true" />
        <activity
            android:name=".AdminDash"
            android:exported="true" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>